// Leaflet 2.0-like Icon.Default (TypeScript stub)
import { Icon, type IconOptions } from './Icon';

export class IconDefault extends Icon {
  constructor(options?: IconOptions) { super(options); }
}

