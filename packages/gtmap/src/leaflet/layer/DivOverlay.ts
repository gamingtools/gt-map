import Layer from '../../api/layer';
import { notImplemented } from '../util';

export class DivOverlay extends Layer {
	onAdd(): void {
		notImplemented('DivOverlay.onAdd');
	}
	onRemove(): void {
		/* nothing to clean yet */
	}
}
